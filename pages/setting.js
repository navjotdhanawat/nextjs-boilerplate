import Layout from "../components/mainlayout";

const Setting = () => {
  return <h6>Welcome to Setting</h6>;
};

Setting.getLayout = (page) => <Layout meta={{}}>{page}</Layout>;

export default Setting;
